/** Should have the questionmark type. */
let blacklisted: NeverTyped = {x: '1'};
console.log(blacklisted);

/** Should have an ordinary type. */
let otherlib: OtherLib = {y: '1'};
console.log(otherlib);
